import { Injectable } from '@nestjs/common';
import { RegisterDto } from './dto/register.dto';

@Injectable()
export class AuthService {
  /*
    1. check email already exist
    2. Hask the password
    3. Create user
    4. Generate JWT
    5. Return token as response
  */
}
